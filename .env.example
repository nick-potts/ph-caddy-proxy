# PostHog Caddy Proxy Configuration
# Copy this file to .env and update with your values

# Your tracking domain (e.g., tracking.example.com)
TRACKING_DOMAIN=tracking.example.com

# PostHog host (default: us.i.posthog.com for US region)
# For EU region, use: eu.i.posthog.com
POSTHOG_HOST=us.i.posthog.com

# PostHog assets host (default: us-assets.i.posthog.com for US region)
# For EU region, use: eu-assets.i.posthog.com
POSTHOG_ASSETS_HOST=us-assets.i.posthog.com

# SSL/HTTPS configuration (enabled by default)
# Set to false for local development or when behind another SSL termination proxy
SSL_ENABLED=true

# Enable debug mode to see generated Caddyfile
DEBUG=false